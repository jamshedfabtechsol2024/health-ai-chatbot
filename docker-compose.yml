services:
  health_ai:
    build: .
    container_name: health_ai
    restart: always
    ports:
      - "8001:8001"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
